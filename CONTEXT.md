# RuntimeRogue ドメイン用語集

JavaScriptコードを書いて敵と戦うブラウザ版ローグライク（RuntimeRogue）のドメイン用語集。

## Language

**バイト**:
敵を1体倒す（＝ステージをクリアする）たびに獲得する、アカウント単位の永続的なメタ進行通貨。本編プレイのみが対象で、チュートリアルでは獲得しない（既存のプレイ履歴`recordClear`/`recordGameOver`と同じ前提）。`localStorage`に保存され、そのランがゲームオーバーになっても失われない。アンロック関数（`enemyHp()`など）の永続購入に使う。表示は1000区切りでKB/MB風にスケールするが、内部の数値は常に10進の整数で管理する（ゲームバランス調整のしやすさを優先し、実際のバイナリ単位1024には合わせない）。ステージクリアのたびに静かに加算されるが、UI上での獲得表示はラン終了時（全クリア or 敗北）にそのラン中の獲得差分だけをまとめて見せる（各ステージクリア画面では表示しない）。全クリア時は、ステージごとの直線報酬とは別枠で「ボス撃破ボーナス」（一律固定額、ジッターなし）が上乗せされる。
_Avoid_: コア（設計初期の仮称。バイトに統一）, コイン（このゲームでは「コインシステム」という機能全体の通称として使われてきたが、通貨そのものの名前としては使わない。戦闘中だけ使う別の通貨と紛らわしいため）

**アンロック関数**:
バイトを消費して永続的に解放する、コードから呼び出せる状態読み取り・操作関数（`enemyHp()`, `myDeck()`, `endTurn()` など計16種）。情報の価値に応じて個別に価格が異なる（均一価格ではない）。一度アンロックすると不可逆（売り戻し不可）。購入は即座にゲーム内の挙動へ反映される（未購入時は全てロック状態）。
_Avoid_: 特になし。既存コードでは`UnlockFunctions`型のフィールド名で呼ばれる。

**キャッシュ**:
ステージクリアのたびにバイトと同時に獲得する、ラン単位（揮発性）の通貨。ステージをまたいで貯まるが、ランが終了（勝利・敗北いずれも）するとリセットされ、次のランには持ち越せない。バイトと違い、獲得のたびにステージクリア画面でその場で表示する。パッケージマネージャでの購入に使う（割り込みでも増減する場合がある）。
_Avoid_: 戦闘内通貨（設計時の説明的な仮称。キャッシュに統一）

**弱点属性**:
Bug Injectorの戦闘固有の概念。3種のエラー種別（`TypeError`/`RangeError`/`SyntaxError`）のうちの1つ。**全ステージ共通で、戦闘開始時および毎ターン終了時にランダムに再抽選される**（敵ごとの固定値ではない）。`debug()`が返すエラーログ配列（30件、終盤ステージではさらに増える場合あり）を集計し、最も出現回数が多い種類が弱点、最も少ない種類が対極。弱点系カードは、対象属性が一致すれば大ダメージ、不一致でも最低限のダメージが出る（外してもデメリットはない設計）。ターンごとに変わるため、`debug()`は毎ターン読み直す前提の設計（コスト1）。
_Avoid_: 毒/バグ（設計初期に「毒」を「バグ」へ改名し持続ダメージ型の構想があったが、キャラコンセプトを弱点解析型へ全面変更したため未使用。この用語系列はコード上にも残っていない）

**内部seed**:
RNG Crackerの戦闘固有の概念。見た目は`Math.random()`のギャンブルカードだが、実際は決定論的な線形合同法（LCG）で管理された内部状態（整数）。`rngSeed()`で読める（常時ON・無料）が、seed自体は確率（%）ではなく「確率判定に使われる前の生の数値」であり、各カードが持つしきい値と比較して初めて成功/失敗が決まる。ギャンブル系カードを呼ぶたびに1ステップ進み、`skipRoll()`で効果なしにステップだけ進めることもできる。公式さえ分かれば次の出目を事前に計算できる（Bug Injectorの弱点属性と同じ「調べれば確実」という設計思想）。
_Avoid_: 確率、乱数値（seedそのものは確率ではないため、この用語系列とは区別する）

**マップ・ノード**:
チュートリアルを除く通常プレイの進行構造。8フロア＋ボス（固定）からなる分岐グラフで、各フロアに2〜3個のノードがある。ノードには戦闘・パッケージマネージャ・割り込みの3種類があり、プレイヤーは訪問済みノードから接続されている次フロアのノードだけを選んで進める（フロアの全ノードへ自由に飛べるわけではない）。1フロア目は全ノードが戦闘固定。
_Avoid_: ステージ（`STAGES`配列そのものを指す既存用語と紛らわしいため、マップ上の1マスは「ノード」と呼ぶ。戦闘ノードが内部的にどのステージ＝敵と対応するかは`stageIndex`で持つ）

**パッケージマネージャ**:
キャッシュを消費してデッキを整備する、ショップ種別のノード。インストール（カード購入・6枚から指定して購入）・デッドコード削除（カード除去。このゲーム唯一の除去手段）・リファクタリング（カード強化。スターター/Commonのみ対象）・プラグイン購入の4つの機能を持つ。
_Avoid_: ショップ（メインメニューからバイトで購入する既存の🛒ショップ画面と紛らわしいため、マップ上のこちらは必ず「パッケージマネージャ」と呼ぶ）

**プラグイン**:
パッケージマネージャで購入する、ラン終了まで持続する受動効果（レリック相当）。全キャラクター共通の単純なステータス修正のみで、条件付きの能動的な効果は現状ない。割り込みの選択肢で無料（リスク付き）で手に入ることもある。

**割り込み**:
マップのイベント種別のノード。2〜4択のテキスト選択で、コードを書く要素は含まない。バイトは戦闘勝利時のみ獲得できる仕様のため、割り込みの報酬にバイトは含まれない（キャッシュ・プラグイン・HP増減・カード強化・次戦闘限定の一時効果のみ）。
_Avoid_: イベント（一般的すぎる語のため、ゲーム内の他の意味との混同を避けてドキュメント上は「割り込み」で統一する。コード上の変数名等は`event`のままでよい）

**リファクタリング**:
パッケージマネージャでカードを強化する機能の名称。対象はスターター・Commonカードのみ。**個々のカードのコピー単位ではなくCardId単位**で強化される（同じカードを複数枚持っていれば、リファクタリングした瞬間に全コピーが同時に強化される）。

**デッドコード削除**:
パッケージマネージャでデッキからカードを1枚取り除く機能の名称。このゲームで唯一のカード除去手段。
_Avoid_: ガベージコレクション（Object Breakerの`decay`ギミックの通称として既に使われているため、カード除去にはこの語を使わない）

**レッスン**:
バイトで購入する、構文/概念単位（代入・分割代入など）の読み物コンテンツ。個々のアンロック関数への「アクセス権」とは別物で、既にアンロック済みの関数の実践的な使い方を教える。複数の関数にまたがりうる概念でも、レッスンごとに代表関数を1つ固定して例文を作る（例: 「代入」レッスンは`enemyIntent()`を例に採用）。アンロック関数と紐付くレッスンは、対象の代表関数がアンロックされていることが購入の前提条件。一方`function`・`includes`のような、ゲーム内アンロック対象ではない一般的なJS構文のレッスンは前提条件を持たず常に購入可能。購入後は🛒ショップ・📚レッスン一覧（購入済みのみを表示する閲覧専用一覧。戦闘中HUDとメインメニュー双方からアクセス可）・📖関数一覧モーダル（前提関数を持つレッスンのみ、対応する関数の行から）のいずれからも読み返せる。表示形式は場所で異なり、メインメニュー側では内容モーダル、戦闘中はキャンバス上の「レッスンウィンドウ」（エディタと同様にドラッグ・リサイズでき、✕で閉じるまで残る）として開く。
_Avoid_: チュートリアル（既存の`TUTORIAL_STEPS`によるオンボーディング機能と紛らわしいため、この用語では呼ばない）
